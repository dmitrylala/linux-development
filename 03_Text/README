## Обработка текстов и сценарии

Команды:
```bash
# Сборка докер образа
make build

# Запуск контейнера. Сборка есть в зависимостях
make run
```

Далее в контейнере тестируем программу:
```bash
cal | ./randomize.sh 0.1
```
